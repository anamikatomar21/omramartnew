import {
  useEffect,
  useState,
} from 'react';

import type { NextPage } from 'next';
import Head from 'next/head';
import Router, { useRouter } from 'next/router';
import toast, { Toaster } from 'react-hot-toast';

// import styles from '../styles/Merchant/login.module.scss'
import UserLayout from '../components/User/Layout';
import { handleSignupState } from '../redux/actions/auth';
import {
  useAppDispatch,
  useAppSelector,
} from '../redux/hooks';
import styles from '../styles/Merchant/signup.module.scss';

const SignUp: NextPage = () => {
  const router = useRouter();
  const dispatch = useAppDispatch();

  const [email, setEmail] = useState<string>("");
  const [password, setPassword] = useState<string>("");
  const [role, setRole] = useState<string>("Admin");

  const { error,user, isAuthenticated} = useAppSelector((state) => state.user);
 console.log(user)
 console.log(isAuthenticated)



  const handleLogin = (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    setRole("Admin");
    dispatch(handleSignupState({
      email,
      password,
      role,
    }));
  }
   

    // );
  console.log(role);

  useEffect(() => {
    if (error) {
      toast.error((error as any).message);
    }
    // toast.success("registered successfully")
    // router.replace('/onboarding/profile')

    // if (data) {
    //   toast.success("register Successfull go onboarding");

    //   router.push(`/onboarding/profile`);
    // }

    if (isAuthenticated) {
      
   
      toast.success("upload Successfull");
      Router.push('/onboarding/profile')
    }
  }, [error, isAuthenticated,user]);


  return (
    <div className="">
      <Toaster position="bottom-center" />
      {/* <UserLayout> */}
        <Head>
          <title>Create Next App</title>
          <meta name="description" content="Generated by create next app" />
          <link rel="icon" href="/favicon.ico" />
        </Head>

        <section className={styles.SectionBox}>
          <div className={styles.imgBx}>
            <img src="/omratrade/omra.png"  className={styles.img_signup} alt="girl" />
          </div>

          <div className={styles.contentBx}>
            <div className={styles.formBx}>
              <h2>Sign up</h2>
              <form onSubmit={handleLogin}>
                <div>
                  <span>Email id</span>
                  <input
                    className={styles.InputBox}
                    type="email"
                    id="email"
                    onChange={(e) => setEmail(e.target.value)}
                  />
                </div>
                {/* <div >
            <span>Username</span>   
            <input className={styles.InputBox} type="text" name=""/>
          </div> */}
                <div>
                  <span>Password</span>
                  <input
                    className={styles.InputBox}
                    type="password"
                    id="password"
                    onChange={(e) => setPassword(e.target.value)}
                  />
                </div>

                <div>
                  <span>Confirm Password</span>
                  <input
                    className={styles.InputBox}
                    type="password"
                    id="password"

                    // onChange={(e) => setPassword(e.target.value)}
                  />
                </div>
                <input type="submit" value="Sign up" name="" className ={styles.buttonsection} onClick={()=>{alert("Login")}}/>

               
                  {/* <input type="submit" value="Sign in" name="" className ={styles.buttonsection}/> */}
                  {/* <Link href="onboarding/profile"> */}
                    {/* <button
                      type="submit"
                      value="Sign in"
                      className={styles.buttonsection}
                      
                    >
                      <a>Sign Up</a>
                    </button> */}
                  {/* </Link> */}
               
                {/* 
          <div >
            <p>Don't have an account?<a href="sign_up">Sign up</a></p>
          </div> */}
              </form>
            </div>
          </div>
        </section>
      {/* </UserLayout> */}
    </div>
  );
};

export default SignUp;
