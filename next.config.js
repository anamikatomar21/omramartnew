/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','merchantapi.elaundry.co.in','www.apiconnecto.com'],
  },
}

module.exports = nextConfig
